Searches for exact DNA sequence matches in a collection of proteins. Allows for multiple concurrent requests.

# Dependencies

- Python 3
- pip
- virtualenv

# Install

- `virtualenv venv && . venv/bin/activate` # Make sure it's python3
- `pip install -r requirements.txt` # mysqlclient only needed for production

# Local development

`cd src && env DEBUG=TRUE SECRET_KEY="secret" SQLALCHEMY_DATABASE_URI="sqlite:///:memory:" FLASK_APP=main.py flask run`

Open http://127.0.0.1:5000/

# About

Has a limit of a 500 character sequence
